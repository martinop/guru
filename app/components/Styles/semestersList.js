export default (theme) => ({
	gridLoading: {
		justifyContent: 'center',
		alignItems: 'center',
		display: 'flex',
		padding: 30,
	},
	pensumProgress: {
		margin: `0 ${theme.spacing.unit * 2}px`,
	},
});
